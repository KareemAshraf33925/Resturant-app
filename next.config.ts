import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  output:"export",
  assetPrefix:"/Resturant-app/",
  basePath:"/Resturant-app",
  images:{
    domains:["KareemAshraf33925.github.io"]
  }
};

export default nextConfig;
